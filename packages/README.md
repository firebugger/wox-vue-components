# wox-vue-components

## Usage

* 安装 `packages` 依赖

```
$ npm run install
```

* 更新 `packages` 依赖

```
$ npm run updated
```

* 构建 `packages`

```
$ npm run build
```

* 发布 `packages`

```
$ npm run publish
```
